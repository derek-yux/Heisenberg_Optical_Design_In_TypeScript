services:
  ml-optimizer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quantum-ml-optimizer
    ports:
      - "5001:5000"
    volumes:
      - ./models:/app/models
      - ./training_data.json:/app/training_data.json
    environment:
      - FLASK_ENV=development
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    networks:
      - quantum-network

networks:
  quantum-network:
    driver: bridge